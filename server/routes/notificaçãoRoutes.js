import express from "express";

const reviewRequests = express.Router();

reviewRequests.post(

);


reviewRequests.get(

);

reviewRequests.patch(

);

reviewRequests.delete(

);

export default { reviewRequests };