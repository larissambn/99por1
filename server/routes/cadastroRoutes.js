import express from "express";

const signUpRoutes = express.Router();

signUpRoutes.post(

);


signUpRoutes.get(

);

signUpRoutes.patch(

);

signUpRoutes.delete(

);

export default { signUpRoutes };