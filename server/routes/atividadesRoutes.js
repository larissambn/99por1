import express from "express";

const volunteerRoutes = express.Router();

volunteerRoutes.post(

);


volunteerRoutes.get(

);

volunteerRoutes.patch(

);

volunteerRoutes.delete(

);

export default { volunteerRoutes };